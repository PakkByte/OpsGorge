name: OpsGorge — Validate Policy & Self-Tests

on:
  pull_request:
    branches: [ main ]

jobs:
  validate:
    runs-on: windows-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Placeholder self-tests (replace later with your real script)
      - name: Schema + self-tests (placeholder)
        shell: pwsh
        run: |
          Write-Host 'Schema/self-tests placeholder running...'

      - name: Enforce Definition of Done (Core)
        shell: pwsh
        env:
          CI_BOOTSTRAP: "1"   # allow soft-pass during bootstrap
        run: |
          Write-Host "Checking for scripts\check-dod.ps1..."
           = Join-Path D:\Repos\OpsGorge 'scripts\check-dod.ps1'
          if (-not (Test-Path )) {
            Write-Host "Not found: "
          } else {
            Write-Host "Found: "
          }

          # Run DoD and capture exit code
          pwsh -File  -RepoRoot D:\Repos\OpsGorge
           = 0

          if ( -ne 0) {
            if ( -eq "1") {
              Write-Host "Bootstrap soft-pass (check-dod exited )."
              exit 0
            } else {
              Write-Error "DoD failed with exit code ."
              exit 
            }
          } else {
            Write-Host "DoD passed."
          }
