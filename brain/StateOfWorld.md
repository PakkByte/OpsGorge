Readiness: 80% (C+ guardrails live; PL/DoD enforced in CI)
Top Risks:
- Secret false positives (tighten allowlist as needed)
- Branch protection drift after renames
Next:
- Notion + n8n read-only pulses
- Add schema tests + sample trees
