# Performance Loop — Global
- After substantial output: run one [SELF EDIT] using the PL mini-rubric.
- If PL < 9/10 → revise; else append “Next steps (owner@date)” and stop.
- Use DEEP PL only on explicit request; default is one pass.
